# Deployment
Deploy `apps/web` to Vercel. Deploy the API to a managed Node runtime. Use managed PostgreSQL with TLS, backups and point-in-time recovery. Store all secrets in platform secret management. Run migrations during controlled releases. Configure monitoring, WAF/rate limits, error reporting and SIEM before production.
